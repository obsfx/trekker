{
  "name": "trekker-monorepo",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "dev:cli": "bun run --cwd packages/cli dev",
    "dev:webapp": "bun run --cwd packages/webapp dev",
    "build:cli": "bun run --cwd packages/cli build",
    "build:webapp": "bun run --cwd packages/webapp build"
  }
}
